#!/usr/bin/php -q
<?php
require dirname(__FILE__) . '/classes/daemon.bootstrap.php';
require dirname(__FILE__) . '/../config/config.wgate.php';

//global $db_options, $amqp_options, $amqp_queues_routings, $queue_max_workers, $daemon_options;

//$daemon = new daemonGate($daemon_options, $amqp_options, $amqp_queues_routings, $queue_max_workers, $argv);
//$daemon->start();